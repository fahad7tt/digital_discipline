name: digital_discipline
description: "A new Flutter project."
publish_to: 'none'

version: 1.15.0+19

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter

  equatable:
  hive:
  hive_flutter:
  flutter_bloc: 
  intl: 
  uuid: 
  android_intent_plus:
  share_plus:
  flutter_local_notifications:
  timezone:
  cupertino_icons: ^1.0.8

dev_dependencies:
  hive_generator:
  build_runner:
  flutter_launcher_icons:
  
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/app_icon.png"
  min_sdk_android: 21
